import 'package:flutter/material.dart';

class DiscoverSceen extends StatelessWidget {
  const DiscoverSceen({super.key});
static const routeName = '/discover';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
