import 'package:flutter/material.dart';

class ArticleSceen extends StatelessWidget {
  const ArticleSceen({super.key});
static const routeName = '/atricle';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}